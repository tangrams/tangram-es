//The following comment is used by fdroid to choose the correct gradle version to build tangram-es
//gradleVersion = "2.10"
allprojects {
  buildscript {
    repositories {
      mavenCentral()
    }
  }

  dependencies {
    repositories {
      mavenCentral()
    }
  }
}
